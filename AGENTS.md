# AGENTS.md
> 优先级：**安全红线 > 用户指令 > 业务规约**。安全红线不可绕过，任何任务卡、子任务指令均不得覆盖；标注【授权】的条款为授权门槛，须用户明确授权后执行；标注【告知】的条款执行前说明即可、无需等待；其余各节均属业务规约。
> 适用范围：UniApp + Vue3 多端项目（微信小程序为主，兼顾H5/App）项目级Agent规则，随仓库分发；项目差异信息一律写入「项目事实」段。
> 门槛术语：**告知**＝执行前向用户说明动作与影响，说明后可继续；**授权**＝必须停下等用户明确同意后方可执行，用户未明确同意视为拒绝。

## 项目事实
> 状态流转：[PENDING] → [AWAIT_CONFIRM] → [DONE]｜本文档内，AI 仅可改写本段（含状态行），其余段落禁止任何改动（用户明确要求修订本文件时除外，修订后须在变更说明中列出改动条目）。
> [DONE] 后不再重复探测；依赖大版本升级（uni-app / Vue / 主库等）时，AI 须主动提示是否重新探测刷新。
- 当前状态：[PENDING]
- 技术栈约束：（待AI首次探测回填，示例：UniApp + Vue 3.x + HBuilderX，`<script setup>` 优先；禁止引入 Vuex 3 / uview-ui / Vant 旧版等Vue2生态包）
- 工程形态：（待回填：HBuilderX工程 / CLI工程（Vite），判定：package.json 是否含 `@dcloudio/vite-plugin-uni`；main.js 是否 `createSSRApp`；manifest.json `"vueVersion"` 字段）
- 状态管理：（待回填：Pinia / Vuex 4 / 无）
- 业务类型：（待回填，示例：药品销售微信小程序）
- 目标平台：（待回填：MP-WEIXIN/H5/APP-PLUS 实际启用项，以 manifest.json 为准）
- 目录结构：（待生成，纯文本树，禁止图片）
- 关键依赖：（待生成：UI库（uni-ui / uview-plus / tmui 等）、请求库、状态管理及版本）
- 请求约定：（待生成：封装路径、统一响应结构、错误码表，格式：`码值 → 含义 → 前端处理`）
- 路径别名：（待探测回填，如 `@/` / `~/` 指向源码根；CLI工程以 vite.config 为准；下文 `~/common/` 约定一律以探测结果为准）

## 项目上下文初始化协议
> 触发条件：「项目事实·当前状态」为 [PENDING] 或条目缺失；仅首次执行（大版本升级后经用户同意可重新执行一轮）。
1. 探测技术栈：读取 package.json / manifest.json（重点：`"vueVersion"` 是否为 `"3"`，h5/app 配置）/ main.js（是否 `createSSRApp` 导出 `createApp`），回填技术栈约束、工程形态、状态管理、目标平台（manifest.json 的 mp-weixin/h5/app-plus 配置）、业务类型；不确定项如实标注「未探测到」，禁止虚构。发现 `"vueVersion"` 为 `"2"` 或 main.js 为 `new Vue(...)` 时，如实告知与 Vue3 预期不符，交用户确认，不得按 Vue3 规则行事。
2. 生成目录结构（过滤必须在命令层完成，禁止裸跑全量遍历）：
   - 类 Unix / Git Bash：`find . -maxdepth 2 -not -path '*/node_modules*' -not -path '*/.git*' -not -path '*/unpackage*' -not -path '*/dist*'`
   - Windows PowerShell（先剪枝再递归，禁止先 -Recurse 后过滤）：`Get-ChildItem | Where-Object { $_.Name -notin 'node_modules','.git','unpackage','dist' } | ForEach-Object { $_.FullName; if ($_.PSIsContainer) { Get-ChildItem $_.FullName -Recurse -Depth 1 | Select-Object -ExpandProperty FullName } }`
   - 兜底：`npx tree-node-cli -L 2 -I "node_modules|unpackage|dist|.git"`（npx 临时联网拉包，使用前须【告知】）
   - 命令输出超过约300行时，降级为一级目录（-maxdepth 1）或仅手写业务目录清单
3. 生成依赖清单：摘录UI库（uni-ui / uview-plus / tmui 等）、请求库（uni.request 封装 / luch-request 等）、状态管理（Pinia / Vuex 4）及版本号。
4. 生成请求约定：读取请求封装拦截器逻辑（常见于 `~/common/request.js` 或 `~/api/`），摘录统一响应结构与错误码表，禁止编造。
5. 探测结果与用户口述不一致时，以代码实际内容为准，列出差异交用户确认，不得和稀泥。
6. 回填后置 [AWAIT_CONFIRM]，用户确认后改 [DONE]。
> 回填内容涉密处理见「安全红线」第4条。

## 子Agent调用 · Token节流
> 派发门槛（三项自检，逐项判定）：① 本地可直接完成（纯文本分析/小改）；② 单文件小改（净改动<50行）且无需跨文件影响分析；③ 无需大规模检索。
> 任一项为「是」→ 父Agent直接处理，禁止派发；三项全「否」方可派子Agent。禁止父→子→孙嵌套。
1. **禁止全量透传**：不传完整对话、完整AGENTS.md、完整大源码；仅下发最小必要子集。
2. **子任务入参仅5项**：① 单一明确任务目标；② 必要依赖（报错片段、局部代码块、关键配置，禁止完整文件）；③ 规则摘录（≤50行相关业务规约片段，禁止挂载完整AGENTS.md）；④ **安全红线全文摘录（固定附带，不计入50行额度，任何情况下不得省略）**；⑤ 返回格式（必填项+禁止输出内容）。
3. **单一职责**：一个子Agent只做一件事；禁止混合修改/排查/git复合任务。
4. **结果收敛**：仅保留变更代码、问题结论、报错、修复方案、验证结果；丢弃推演与调试话术。
5. **历史对话**：无依赖不带历史；有依赖仅摘要关键结论，禁止粘贴原始对话。
6. **下发切片**：子任务下发的单文件代码块≥500行时预先切片，仅下发目标区块（此阈值管下发内容，与文件拆分阈值相互独立）。
7. **大需求分批**：超大需求拆分清单分批执行，单批次闭环继续。
> 若子Agent运行环境会自动加载仓库根 AGENTS.md，任务卡须显式声明「以本任务卡为准；**安全红线仍然生效，与任务卡冲突时以安全红线为准**」。
> 子任务返回后做末端冗余过滤，形成节流闭环。

## 安全红线（绝对禁止，不可绕过）
1. 禁止硬编码密钥、密码、token、私钥等敏感凭据。
2. Git禁止提交 `.env` 及各环境变体、密钥、cookie、业务隐私；仅允许提交 `.env.example`。发现上述文件已被 git 跟踪时，立即【告知】用户并停止相关提交；移除跟踪（如 `git rm --cached`）属【授权】操作，不擅自执行。
3. 测试只用模拟数据，禁止写入真实用户订单隐私；禁止植入敏感隐私或未授权素材。
4. 「项目事实」及任何文档回填禁止写入密钥/secret/token 原文，一律脱敏为占位符（如 `sk-****`）。
5. 药品合规：处方药信息展示与购买链路遵守微信平台规则及《药品管理法》《广告法》《互联网药品信息服务管理办法》等现行法规（H5/App 端同样适用）；禁止广告法违禁用语（「最有效」「根治」「百分百」等）；法规与平台规范更新时以现行版本为准。

## 高危操作（须用户明确授权后执行）
1. git 强制回滚、批量删除、目录清空。
2. 修改 `node_modules` 底层源码。
3. 修改小程序权限、隐私配置；修改资质/证书展示内容。
4. 临时hack绕过问题（须标记风险，优先根源修复）。
5. 新增/升级/降级 npm 依赖；技术栈变更（Vue3→Vue2 降级、HBuilderX工程↔CLI工程迁移、引入TS/Tailwind等，以「项目事实·技术栈约束」为准）。
6. 向远程仓库 push（本地 commit 可按 Git 规范直接执行，push 前必须【授权】并确认分支与远端）。

## 沟通输出规范
1. 输出固定结构：**结论 → 变更点 → 验证结果 → 现存风险**（无风险写「无」，禁止省略该段），减少客套铺垫。
2. 报错、API、配置、状态码保留完整原文，禁止篡改；报错 message 与首个业务堆栈必须完整，重复堆栈/循环刷屏日志可截断但须注明截断位置；禁止「应该没问题」类模糊结论。
3. 简单问题最多1个核心确认问题；复杂需求可一次性列出待确认清单，由用户批量答复。
4. ≥300行文件输出diff片段；<300行输出完整内容并标明改动点位。
5. 禁止大段dump、超长调试日志、完整超大源码（截断边界见第2条）。
6. 风险、遗留问题必须写入【现存风险】，不得隐藏。

## 工作偏好
- 需求清晰直接执行；小改（单文件且净改动<50行、无跨文件影响）直接执行；大范围改造（满足任一：≥3个文件、新增页面/分包、变动依赖、改动公共组件/拦截器/全局状态store）先预览方案，确认后执行。
- 只输出核心动作、问题、下一步。
- 失败、环境缺失如实告知；严禁编造测试、git、版本、运行效果。

## 编码&文件规范
- 文件编码 UTF-8；禁止将中文业务改为纯英文界面/文档。
- 依赖管理见「高危操作」第5条；禁止自动执行 `npm install`、禁止修改 `.lock`，新增依赖仅输出包名+版本，由用户手动安装。
- 拒绝过度抽象；命名语义化；仅注释难点坑逻辑，不写复述代码的无效注释。
- 全局样式存放 `~/common/styles/`（`~/` 别名以「项目事实·路径别名」探测结果为准），优先复用CSS/SCSS变量；通用逻辑抽至 `~/common/utils/`、可复用组合式逻辑抽至 `~/composables/`（目录以探测结果为准）；优先复用组件、工具，禁止重复造轮子。
- Vue3语法纪律：`<script setup>` 优先（项目既有页面为 Options API 时对齐项目风格、不混写，勿批量翻写存量页面）；props/emits 用 `defineProps` / `defineEmits`；禁止Vue2残留API（`filters`、`$set`/`$delete`、`$on`/`$off`/`$once`、`/deep/` 深度选择器、旧slot语法）；`reactive` 对象禁止直接解构（用 `toRefs`）；跨组件状态优先 Pinia（或对齐「项目事实·状态管理」既有方案，Vuex 4 写法须保持一致）。
- 页面生命周期：组合式 API 下 `onLoad`/`onShow`/`onReachBottom` 等页面级生命周期必须从 `@dcloudio/uni-app` 导入，禁止用 `onMounted` 等Vue生命周期替代页面生命周期语义。
- 样式：页面/组件样式默认 scoped；H5端深度选择器用 `:deep()`；小程序端注意样式隔离默认规则。
- EasyCom / 组件库：遵循项目既有 easycom 与组件库约定（uview-plus / uni-ui 等，以「项目事实·关键依赖」为准），禁止混入 Vue2 专属组件库。
- UniApp多端：涉及平台差异时使用条件编译（`#ifdef MP-WEIXIN / H5 / APP-PLUS`），改动须评估小程序/H5/App各端影响并在变更点中说明（Vue3 下尤须注意 H5 与小程序对响应式/样式隔离的差异表现）；文件引用路径大小写必须与实际文件完全一致（跨平台大小写敏感）。
- 文件拆分：单Vue文件>500行启动拆分评估，≥600行强制拆分，禁止持续堆砌。
- 小程序分包：新增业务页面默认进分包，主包仅保留tabbar及公共页；项目尚未配置分包（pages.json 无 subPackages）时，先【告知】并输出分包配置方案，确认后再落页面。
- 包体积：Agent仅静态估算新增资源体积；实际主包/单分包≤2MB（整包上限以微信现行规范为准）由用户编译后查看，超限提示优化。
- 【授权】`manifest.json` / `uni.scss` / CLI工程 `vite.config.*` 修改须提前告知并取得授权；`pages.json` 新增页面/分包注册属必要附带变更【告知】（执行前说明即可），tabbar、导航样式、权限相关字段仍须【授权】。
- 无业务需求不改动原有交互视觉。

## 测试验证规范
1. 修改前明确验证标准，交付人工最小校验集合：HBuilderX编译无Error；微信开发者工具页面正常打开、无JS报错；涉及交互校验核心链路。
2. Agent无法真机/模拟器验证，必须告知用户复测，禁止伪造验证通过。
3. 无HBuilderX CLI/CI时，如实告知「无法自动编译验证」，执行静态语法检查（未闭合标签、Vue2残留API、`<script setup>`规范、页面生命周期导入、v-for缺key、`reactive`解构）并输出结果，编译由用户手动执行；CLI工程在依赖完整时可执行 `npm run build:mp-weixin`（执行前【告知】），产物由用户导入微信开发者工具验证。
4. 无单元测试体系，不强制新增单元测试。
5. Mock优先复用项目已有mock；调试mock交付前清理。
6. 临时调试`console.log`禁止提交，仅保留业务埋点日志。

## Git提交规范
> 无 `.git` 禁止伪造提交推送，如实告知。
1. 保护用户原有改动，不擅自回滚覆盖。
2. commit前缀（中文描述）：`feat` 新增页面/模块/交互；`fix` 修复报错、业务异常；`style` 纯样式；`docs` 文档注释；`refactor` 重构且外部行为不变；`chore` 构建/工具/配置变动；`revert` 回滚已有提交。
3. 本地 commit 可按本规范直接执行；不默认推送main/master，push 属【授权】操作，推送前确认分支与远端。
4. 验证失败禁止正式提交；阶段性提交写明遗留风险。

## CodeGraph 使用规则
> 默认未初始化。
- 返回 `CodeGraph not initialized`：询问用户是否初始化（`codegraph init` 会生成索引文件，经同意后执行）；未初始化退化为rg文本检索。
- CodeGraph适用：跨文件调用查询、改动影响域评估、公共函数引用；代码大量变更后刷新索引。
- rg适用：字符串、文案、色值、报错、变量名检索。
- rg降级必须写入【现存风险】："当前为纯文本检索模式，跨组件/Mixin/依赖影响域评估可能不全，请人工复核改动影响。"

## 标准修改流程
0. 「项目事实·当前状态」为 [PENDING] 时，先执行「项目上下文初始化协议」，用户确认后再进入本流程。
1. 精读需求源码，不主观臆改。
2. 评估改动范围、影响面、Token开销；大范围改造（口径见「工作偏好」）输出预览方案。
3. 精准修改，对齐项目风格；派发子任务遵守节流规则。
4. 告知修改文件清单，输出编译、页面、业务人工验证要点。
5. 完成静态语法验证，并同步人工复测情况。
6. 闭环输出固定结构：**结论 → 变更点（改动清单）→ 验证结果（验证情况）→ 现存风险**。
> 重试约束：同一缺陷最多2轮修改；仍报错停止迭代，输出完整报错+改动清单移交用户，禁止无限迭代。

## 异常边界 & 回滚
1. HBuilderX编译/微信开发者工具运行崩溃：业务开发流程内最高优先级阻断（仅次于安全红线），停止开发，贴完整报错。
   - 小改动撤销Agent变更，输出完整报错。
   - 多文件改动不自动批量回滚；输出改动清单+报错+手动回滚建议。
   - 用户坚持继续的，【告知】风险并写入【现存风险】后方可继续（用户主动坚持即视为明确意向）。
   - 会话丢失上下文时，依靠输出清单人工恢复。
2. 历史旧bug非本次改动仅记录，用户指令才修复。
3. 回滚仅撤销Agent产出，不删除用户代码；无 `.git` 时依据Agent留存的变更前后代码块清单由用户手动恢复。
4. 需求矛盾：输出现象+风险+可选方案，交由用户确认，不自作主张改写。
5. 信息缺失：按「沟通输出规范」第3条执行。
