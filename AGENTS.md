# AGENTS.md（UniApp Vue2 小程序）
> 项目级Agent规则；用户指令优先级高于本规则。
> 优先级：**安全红线 > 用户指令 > 业务规约**；安全红线不可绕过。
> 项目：UniApp + Vue2 HBuilderX 药品销售微信小程序；禁止Vue3 / `<script setup>`。
## 子Agent调用 · Token节流（全局最高强制）
> 父Agent三轮自检才可派发子任务；低成本任务父Agent直接处理，禁止无脑派子Agent；禁止父→子→孙嵌套调用。
1. **禁止全量透传**：不传递完整对话、完整AGENTS.md、完整大文件源码；仅下发任务最小必要子集。
2. **子任务入参仅4项**
    - 单一明确任务目标
    - 必要依赖：报错片段、局部代码块、关键配置（禁止完整文件）
    - 按需摘录少量相关规则片段，**禁止挂载完整AGENTS.md**
    - 明确返回格式、必填项、禁止输出内容
3. **单一职责**：一个子Agent只做一件事；禁止混合修改/排查/git复合任务。
4. **结果收敛**：子任务返回丢弃思考推演、调试话术；仅保留变更代码、问题结论、报错、修复方案、验证结果带入上下文。
5. **历史对话**：无依赖任务不带历史；有依赖仅摘要关键结论，禁止粘贴原始对话。
6. **大文件切片**：≥560行Vue/配置，父Agent预先切片，仅下发目标代码区块。
7. **大需求分批**：超大需求输出拆分清单，分批次执行，单批次闭环继续。
> 子任务执行后做末端冗余过滤，形成Token节流闭环。
## 沟通输出规范
1. 输出固定结构：**结论 → 变更点 → 验证结果**，减少客套铺垫。
2. 报错、API、配置、状态码保留原文，不篡改。
3. 报错粘贴完整原始关键信息。
4. 用户疑问最多提**1个核心确认问题**。
5. ≥300行文件输出diff片段；<300行输出完整内容，标明改动点位。
6. 禁止输出大段dump、超长调试日志、完整超大源码。
7. 风险、遗留问题必须写入【现存风险】，不得内部隐藏。
## 工作偏好
- 需求清晰直接执行；高危变更先预览方案，用户确认后执行。
- 只输出核心动作、问题、下一步。
- 失败、环境缺失如实告知；严禁编造测试、git、版本、运行效果虚假信息。
- 任务前评估Token开销，本地可完成不派子Agent。
## 编码&文件规范
- 文件编码 UTF‑8。
- 无确认不新增/升级/降级npm依赖；改动给出风险，用户确认执行。
- **禁止自动执行 `npm install`、禁止修改 `.lock`；新增依赖仅输出包名+版本，由用户手动安装更新lock。**
- 拒绝过度抽象；命名语义化；仅注释难点坑逻辑，不写复述代码的无效注释。
- 全局样式存放 `~/common/styles/`，优先复用CSS变量。
- Vue>560行启动拆分评估，≥600行强制拆分，禁止持续堆砌。
- 无业务需求不改动原有交互视觉。
- 优先复用组件、工具，禁止重复造轮子。
- 通用逻辑抽至 `~/common/utils/`。
- 无授权禁止修改 `pages.json` / `manifest.json` / `uni.scss`；如需修改提前告知。
## 安全红线（绝对禁止，不可绕过）
1. 禁止硬编码密钥、密码、token、私钥等敏感凭据。
2. Git禁止提交 `.env`、密钥、cookie、业务隐私；仅允许提交 `.env.example`。
3. 测试只用模拟数据，禁止写入真实用户订单隐私。
4. 无确认不修改小程序权限、隐私配置。
5. 无确认禁止git强制回滚、批量删除、目录清空等高风险操作。
6. 无确认禁止修改 `node_modules` 底层源码。
## 测试验证规范
1. 修改前明确验证标准；交付人工最小校验集合：
    - HBuilderX编译无Error
    - 微信模拟器页面正常打开，无JS报错
    - 涉及交互校验核心链路
    > Agent无法真机/模拟器验证，必须告知用户复测，禁止伪造验证通过。
	> 无HBuilderX CLI/CI时，告知“无法自动编译验证”，做静态语法检查（未闭合标签、Vue2禁止setup、ES6兼容性）输出检查结果，编译交由用户HBuilderX手动执行。
2. 输出完整原始报错，禁止片段截取；禁止“应该没问题”类模糊结论。
3. 无单元测试体系，不强制新增单元测试。
4. Mock优先复用项目已有mock；调试mock交付前清理。
5. 临时调试`console.log`禁止提交，仅保留业务埋点日志。
## Git提交规范
> 无`.git`禁止伪造提交推送，如实告知。
1. 保护用户原有改动，不擅自回滚覆盖。
2. commit类型前缀（中文描述）
    - `feat`：新增页面/模块/交互
    - `fix`：修复报错、业务异常
    - `style`：纯样式
    - `docs`：文档注释
    - `refactor`：重构，外部行为不变
3. 不默认推送main/master，推送前确认分支。
4. 验证失败禁止正式提交；阶段性提交写明遗留风险。
## CodeGraph 使用规则
> 默认未初始化 CodeGraph
- 返回`CodeGraph not initialized`：询问用户是否初始化；未初始化退化为rg文本检索。
- CodeGraph适用：跨文件调用查询、改动影响域评估、公共函数引用。
- rg检索适用：字符串、文案、色值、报错、变量名检索。
- **rg降级必须写入【现存风险】：“当前为纯文本检索模式，跨组件/Mixin/依赖影响域评估可能不全，请人工复核改动影响。”**
## 标准修改流程
1. 精读需求源码，不主观臆改。
2. 评估改动范围、影响面、Token开销；大范围改造输出预览方案。
3. 精准修改，对齐项目风格。
4. 告知修改文件清单；派发子任务遵守子Agent节流规则。
5. 输出编译、页面、业务人工验证要点。
6. 完成静态语法验证，并同步人工验证复测情况。
7. 闭环输出：**改动清单、验证情况、现存风险**。
> 重试约束：同一缺陷最多2轮修改；仍报错停止迭代，输出完整报错+改动清单移交用户，禁止无限迭代。
## 异常边界 & 回滚
1. 编译崩溃阻断：
    - 小改动撤销Agent变更，输出完整报错。
    - 多文件改动：不自动批量回滚；输出改动清单+报错+手动回滚建议。
    - 会话丢失上下文，依靠输出清单人工恢复。
2. 历史旧bug非本次改动仅记录，用户指令才修复。
3. 回滚仅撤销Agent产出，不删除用户代码。
4. 需求矛盾输出现象+风险+可选方案，交由用户确认，不自作主张改写。
5. 信息缺失最多提1个确认问题。
6. HBuilderX编译/模拟器崩溃为最高优先级阻断，停止开发，贴完整报错。
7. 临时hack绕过问题必须标记风险并经用户确认，优先根源修复。
## 全局禁止行为
- 禁止升级Vue3、切换Vite/TS/Tailwind，禁止启用`<script setup>`。
- 禁止植入敏感隐私、未授权素材。
- 禁止虚构验证、隐瞒失败；不擅自格式化无关文件。
- 禁止将中文业务改为纯英文界面/文档。
- 禁止无意义拆分任务、多层嵌套子Agent；禁止向子Agent透传完整对话、完整AGENTS.md、完整大源码。